<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group
        android:checkableBehavior="single"
        android:id="@+id/nav_group_action">
        <item
            android:id="@+id/nav_finalize"
            android:icon="@drawable/ic_menu_finalize"
            android:title="@string/nav_menu_finalize" />

        <item
            android:id="@+id/nav_peer_sync"
            android:icon="@drawable/ic_menu_sync"
            android:title="@string/nav_menu_peer_sync" />
    </group>

    <group
        android:checkableBehavior="none"
        android:id="@+id/nav_group_external">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_menu_preferences"
            android:title="@string/nav_menu_preferences" />

        <item
            android:id="@+id/nav_about"
            android:title="@string/nav_menu_about" />
    </group>
</menu>
